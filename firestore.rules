rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ユーザー自身のプロフィール（isPremium 等）: 本人のみ読み書き
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
      // ワークアウトログ（総負荷量記録）: 本人のみ読み書き
      match /workoutLogs/{logId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
